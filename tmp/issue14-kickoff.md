Fix Issue 14 (typechecker polymorphic-polarity gap). First do the GLP session-start protocol: read `/Users/udi/Grassroots/GLP/CLAUDE.md`, `docs/typed-glp-manual.md`, and `docs/glp-cheat-sheet.md`; then read the bug report `docs/typechecker-polymorphic-polarity-bug.md` and known-issues `docs/known-issues.md` Issue 14. The bug: when a procedure parameter has a polymorphic element type (`Stream(X)?`, `Channel(X,Y)?`), reader/writer polarity obligations in the body are discharged against the abstract type variable and never re-discharged when `X` is instantiated to a concrete type at a call site, so a producer/consumer duality clash is silently accepted. Differential repro (on disk, untracked): `programs/tests/min_polarity_bug.glp` correctly errors with `(S, S?) not dual`; `programs/tests/min_polarity_bug2.glp` differs only in the consumer's declared type (`Stream(X)?` instead of `Stream(ConsMsg)?`) and wrongly passes. Fix direction (from the report): in the per-instantiation clause-template check, when a parameter type variable is bound to a concrete type, re-run the clause's mode/polarity discharge under that substitution, not only the structural/shape unification — likely in `glp_runtime/lib/analysis/type_checker/type_checker.dart` (per-instantiation / `checkSingleProcedure` path) and/or `param_expansion.dart`. Use `bin/glpc` for fast checks (`printf 'load ../programs/tests/min_polarity_bug2.glp\n:quit\n' | bin/glpc` from `glp_runtime/`). Baseline-before-commit: run `bash test/run_all_tests.sh` (expect 485/485) before and after; the fix is correct when `min_polarity_bug2.glp` reports the `(S, S?) not dual` error AND the suite stays 485/485. Commit the two repro files together with the fix, add a Section-A regression test asserting bug2 now errors, and mark Issue 14 fixed in `docs/known-issues.md`.
