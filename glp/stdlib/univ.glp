% =../2 - Structure decomposition/composition (univ)
%
% Bidirectional conversion between structures and lists.
%
% Composition: list → tuple
%   [foo, a, b] =.. T  →  T = foo(a, b)
%
% Decomposition: tuple → list
%   foo(a, b) =.. L  →  L = [foo, a, b]

% Composition: list → tuple
X? =.. [Y|Ys] :- list(Ys?) | list_to_tuple([Y?|Ys?], X).

% Decomposition: tuple → list
X =.. Y? :- tuple(X?) | tuple_to_list(X?, Y).
