% reverse.glp - List reversal
%
% Efficient list reversal with accumulator
% From Concurrent Prolog book

reverse(Xs, Ys?) :- rev(Xs?, [], Ys).

rev([], Acc, Acc?).
rev([X|Xs], Acc, Ys?) :- rev(Xs?, [X?|Acc?], Ys).

% Test via REPL: reverse([a,b,c], Ys).
