-module(math_reduce).
-export([dispatch/1]).

%% dispatch/1 clauses - FCP-style direct dispatch
%% Pattern matches goal, body executes directly
dispatch(double(X, Y)) :- Y := X? * 2.
dispatch(triple(X, Y)) :- Y := X? * 3.
