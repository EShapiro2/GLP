-module(math_rules).
-export([reduce/2]).

reduce(double(X, Y?), (Y := X? * 2)).
% Note: square requires X? * X? which violates SRSW (reader used twice)
% For now, we'll just test double which works
reduce(triple(X, Y?), (Y := X? * 3)).
