% ip_direct.glp - Inner product (direct execution only)
ip(Xs, Ys, S?) :- ip_acc(Xs?, Ys?, 0, S).

ip_acc([], [], Acc, Acc?).
ip_acc([X|Xs], [Y|Ys], Acc, S?) :-
  number(Acc?) |
  Acc1 := Acc? + (X? * Y?),
  ip_acc(Xs?, Ys?, Acc1?, S).
