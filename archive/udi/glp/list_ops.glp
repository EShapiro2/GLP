% list_ops.glp - List operations
%
% Common list manipulation predicates

% Append two lists
append([], Ys, Ys).
append([X|Xs], Ys, [X?|Zs?]) :- append(Xs?, Ys?, Zs).

% List member check
member(X, [X?|Tail]) :- true.
member(X, [Head|Xs]) :- member(X?, Xs?).

% List length
length([], zero).
length([Head|Xs], succ(N?)) :- length(Xs?, N).
