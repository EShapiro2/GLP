name: glp_multiagent
description: GLP Multi-Window Multiagent Simulation

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0
  path_provider: ^2.1.0

  # Link to existing GLP runtime
  glp_runtime:
    path: ../glp_runtime

  # GrassApp Duo: cross-phone madGLP routing over the Grassroots transport.
  grassroots_networking:
    path: ../networking
  redux: ^5.0.0
  cryptography: ^2.7.0
  sodium_libs: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # GLP scenario sources, bundled for sandboxed platforms (iOS) where the engine
  # cannot read the repo from disk. Copied from programs/ — keep in sync via
  # tool/sync_glp_assets.sh. On macOS the repo on disk is used instead.
  assets:
    - assets/glp/programs/self.glp
    - assets/glp/programs/social/graph/routing/
    - assets/glp/programs/grassapp/
    - assets/glp/programs/social/graph/
    - assets/glp/programs/social/graph/ui/
    - assets/glp/programs/cssn/
    - assets/glp/programs/cssn/ui/
