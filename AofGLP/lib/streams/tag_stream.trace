=== tag_stream.glp ===
Stream tagging for source identification.
Goal: tag_stream(Name, InStream, OutStream)

When multiple streams merge, source identity is lost.
Wraps each message M in msg(Name, M) term.
Tagged streams can be safely merged while preserving source.
