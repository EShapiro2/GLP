% Test: Union alias with overlapping functors
% Expected: FAIL - expanded alternatives overlap (both have foo/1)

TypeA ::= foo(Integer).
TypeB ::= foo(String).

% This should fail: after expansion, both have foo/1
BadUnion ::= TypeA ; TypeB.

procedure test(BadUnion?).
test(foo(X?)).
