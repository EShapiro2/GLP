Nat ::= 0 ; s(Nat).
List ::= [] ; [Any | List].

procedure expect_nat(Nat).
procedure bad(List).

% ERROR: expect_nat expects Nat, but X? is bound to List [] in head
bad([]) :- expect_nat([]).

expect_nat(0).
