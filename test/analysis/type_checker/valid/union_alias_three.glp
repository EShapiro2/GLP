% Test: Union alias with three types
% Expected: PASS - union alias should expand all three types

TypeA ::= a(_).
TypeB ::= b(_, _).
TypeC ::= c.

% Union of three types
ABC ::= TypeA ; TypeB ; TypeC.

procedure process(ABC?).

process(a(X?)).
process(b(X?, Y?)).
process(c).
