% test_reactivation.glp - Test goal reactivation when writer binds
%
% Tests that when a writer X binds, goals suspended on reader X? wake up
%
% Setup:
%   p(X) binds X to 'a'
%   q(X?) reads X and should suspend until X is bound

% p binds X to 'a'
p(a).

% q waits for X to be bound (will suspend if X unbound)
q(X) :- known(X) | execute('write', [X?]), execute('nl', []).
