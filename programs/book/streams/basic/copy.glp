%% Type definitions
Stream ::= [] ; [Any | Stream].

%% Procedure type declarations
procedure copy(Stream, Stream).

%% copy.glp - Stream copy
%% copy(Xs, Ys) :- Ys is a copy of stream Xs

copy([], []).
copy([X|Xs], [X?|Ys?]) :- copy(Xs?, Ys).

%% Example goal: copy([a,b,c], Ys).
