%% length.glp - List length with accumulator

length(Xs, N?) :- length(Xs?, 0, N).

length([], Acc, Acc?).
length([_|Xs], Acc, N?) :- Acc1 := Acc? + 1, length(Xs?, Acc1?, N).

%% Example goal: length([a,b,c,d], N).
%% Expected: N = 4
