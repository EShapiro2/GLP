%% factorial.glp - Factorial computation

factorial(0, 1).
factorial(1, 1).
factorial(N, F?) :-
    N? > 1 |
    N1 := N? - 1,
    factorial(N1?, F1),
    F := N? * F1?.

%% Example goal: factorial(5, F).
%% Expected: F = 120
