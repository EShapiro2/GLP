% struct_demo.glp - Structure demonstration
%
% Shows how to work with compound terms (structures)
% Note: using atoms instead of numbers due to parser limitation

person(alice, age(thirty), city(seattle)).
person(bob, age(twentyfive), city(portland)).

get_age(person(Name?, age(Age?), _), Name, Age) :- true.

get_city(person(Name?, _, city(City?)), Name, City) :- true.

build_person(P) :- P = person(alice, age(thirty), city(seattle)).
