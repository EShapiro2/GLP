% Fibonacci direct execution only (renamed to avoid conflict with fib.glp)
% Base cases
fibd(0, 0).
fibd(1, 1).

% Recursive case
fibd(N, F?) :-
  N? > 1 |
  N1 := N? - 1,
  N2 := N? - 2,
  fibd(N1?, F1),
  fibd(N2?, F2),
  F := F1? + F2?.
