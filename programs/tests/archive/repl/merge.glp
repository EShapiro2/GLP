% merge.glp - Stream merging (N-to-1)
%
% Merge two streams into one by copying elements alternately
% From metainterp_merge_test.dart

% Clause 1: Take from first stream
merge([X|Xs], Ys, [X?|Zs?]) :- merge(Ys?, Xs?, Zs).

% Clause 2: Take from second stream
merge(Xs, [Y|Ys], [Y?|Zs?]) :- merge(Xs?, Ys?, Zs).

% Clause 3: Base case - both empty
merge([], [], []).
