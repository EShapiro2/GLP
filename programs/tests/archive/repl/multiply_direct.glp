% multiply_direct.glp - Scalar multiply stream (direct execution only)
% Renamed to multiplyd to avoid conflict with multiply.glp
multiplyd(_, [], []).
multiplyd(N, [X|Xs], [Y?|Ys?]) :-
  number(N?) |
  Y := N? * X?,
  multiplyd(N?, Xs?, Ys).
