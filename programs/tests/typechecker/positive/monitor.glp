% test: monitor_simple
% expected: pass
% description: Simplified monitor - tests basic stream processing

% Use generic stream instead of IntStream with Integer
MyStream ::= [] ; [_|MyStream].

procedure process(MyStream?, MyStream).

process([], []).
process([X|Xs], [X?|Ys?]) :- process(Xs?, Ys).
