% test: int_list_sum
% expected: pass  
% description: POSITIVE control - stream with constants

MyStream ::= [] ; [_|MyStream].

procedure identity(MyStream?, MyStream).

% Constants at _ positions - correct
identity([], []).
identity([X|Xs], [X?|Ys?]) :- identity(Xs?, Ys).
