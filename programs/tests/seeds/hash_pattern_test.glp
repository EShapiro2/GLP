%% Provenance: salvaged from programs/misc/ — A5 Stage A slice 5 (2026-06-13). Regression seed; not in any suite.
%% Test # as pattern in head
extract_module(M # _, M?).
extract_goal(_ # G, G?).

test_extract(M?, G?) :-
    extract_module(foo # bar(1,2), M),
    extract_goal(foo # bar(1,2), G).
