%% Well-moded merge procedure
%% Uses predefined Stream(X) type from prelude

procedure merge(Stream(_)?, Stream(_)?, Stream(_)).

merge([], Ys, Ys?).
merge([X|Xs], Ys, [X?|Zs?]) :- merge(Xs?, Ys?, Zs).
