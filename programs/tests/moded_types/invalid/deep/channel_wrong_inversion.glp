%% INVALID: Mode error AND SRSW violation
%%
%% Type: Channel ::= ch(Number?, Number)
%% Procedure: bad_channel(Channel?) - input (↓)
%%
%% Mode analysis for ch(Req?, Resp?) in Channel?:
%%   - First arg Number? at ↓: input flips → ↓.flip = ↑, expects writer
%%     Req? is reader at ↑ → MODE ERROR
%%   - Second arg Number at ↓: output preserves → ↓, expects reader
%%     Resp? is reader at ↓ → mode OK
%%
%% SRSW violations: 
%%   - Req? appears but Req never appears → SRSW violation
%%   - Resp? appears but Resp never appears → SRSW violation

Channel ::= ch(Number?, Number).

procedure bad_channel(Channel?).

%% ERROR: Mode error at arg 1 AND SRSW violations for both variables
bad_channel(ch(Req?, Resp?)).
