% multiply_direct.glp - Scalar multiply stream (direct execution only)

procedure multiplyd(Number?, Stream(Number)?, Stream(Number)).
multiplyd(_, [], []).
multiplyd(N, [X|Xs], [Y?|Ys?]) :-
  number(N?) |
  Y := N? * X?,
  multiplyd(N?, Xs?, Ys).
