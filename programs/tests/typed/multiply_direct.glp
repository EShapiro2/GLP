% multiply_direct.glp - Scalar multiply stream (direct execution only)

NumList ::= [] ; [Number | NumList].

procedure multiplyd(Number?, NumList?, NumList).
multiplyd(_, [], []).
multiplyd(N, [X|Xs], [Y?|Ys?]) :-
  number(N?) |
  Y := N? * X?,
  multiplyd(N?, Xs?, Ys).
