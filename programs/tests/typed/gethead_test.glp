% gethead_test.glp - Extract head from a list

exported procedure gethead(Stream(X)?, X).
gethead([H|_], H?).
gethead([], none).

exported procedure test_gethead(Constant).
test_gethead(R?) :- gethead([a,b,c], R).
