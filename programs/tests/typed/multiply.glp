% multiply.glp - Scalar multiply stream

exported procedure multiply(Number?, Stream(Number)?, Stream(Number)).
multiply(_, [], []).
multiply(N, [X|Xs], [Y?|Ys?]) :-
  ground(N?) |
  Y := N? * X?,
  multiply(N?, Xs?, Ys).
