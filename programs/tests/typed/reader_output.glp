% reader_output.glp - Reader in output position binds result correctly

exported procedure unwrap(Stream(X)?, X).
unwrap([X|_], X?).
unwrap([], none).

exported procedure identity(_?, _).
identity(X, X?).
