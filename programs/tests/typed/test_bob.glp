% test_bob.glp - Stream processing with guards

procedure bob(Stream?).
bob([msg(1, Greeting, response(bob, 42)) | Rest]) :-
    string(Greeting?) |
    bob_round2(Rest?).
bob([]).

procedure bob_round2(Stream?).
bob_round2([]).
bob_round2([_|T]) :- bob_round2(T?).
