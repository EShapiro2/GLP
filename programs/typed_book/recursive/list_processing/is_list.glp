%% is_list.glp - Defining a list (AoP 3.11)
%% list(Xs) :- Xs is a list.

%% Uses predefined List type

%% Procedure type declarations
procedure list(List?).
procedure reduce(_?, _).

list([]).
list([_|Xs]) :- list(Xs?).

%% reduce clauses for metainterpreter
reduce(list([]), true).
reduce(list([_|Xs]), list(Xs?)).
