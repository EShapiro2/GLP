%% copy.glp - Stream copying
%% Source: Standard GLP list operations

%% Uses predefined Stream type

%% Procedure type declarations
exported procedure copy(Stream(X)?, Stream(X)).
copy([], []).
copy([X|Xs], [X?|Ys?]) :- copy(Xs?, Ys).

%% Metainterpreter support
exported procedure reduce(_?, _).
reduce(copy([], []), true).
reduce(copy([X|Xs], [X?|Ys?]), copy(Xs?, Ys)).
