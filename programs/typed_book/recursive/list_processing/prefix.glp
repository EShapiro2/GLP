%% prefix.glp - Prefixes of a list (AoP 3.13)
%% prefix(Prefix,Stream) :- Prefix is a prefix of Stream.

%% Uses predefined Stream type

%% Procedure type declarations
procedure prefix(Stream?, Stream?).
prefix([], _).
prefix([X|Xs], [X?|Ys]) :- prefix(Xs?, Ys?).

%% reduce clauses for metainterpreter
procedure reduce(_?, _).
reduce(prefix([], _), true).
reduce(prefix([X|Xs], [X?|Ys]), prefix(Xs?, Ys?)).
