%% self.glp — Village scenario module declaration
%%
%% All CSSN protocol types (UserCmd, UserNotify, ActorChannel, etc.)
%% and NarrativeStream types are inherited from the parent
%% cssn_modules_v2/self.glp via ancestor scoping.

-module(village).
