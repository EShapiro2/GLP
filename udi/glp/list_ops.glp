% list_ops.glp - List operations
%
% Common list manipulation predicates

% Append two lists
append([], Ys, Ys).
append([X|Xs], Ys, [X?|Zs?]) :- append(Xs?, Ys?, Zs).

% List member check
member(X, [X?|_]).
member(X, [_|Xs]) :- member(X?, Xs?).

% List length
length([], zero).
length([_|Xs], succ(N?)) :- length(Xs?, N).
