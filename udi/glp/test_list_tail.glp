% Test general list syntax with tail notation

% Test [H|T] - single element with tail
test_single([X|Xs]) :- test_single(Xs?).
test_single([]).

% Test [X,Y|T] - multiple elements with tail
test_multi([A,B|Rest]) :- test_multi(Rest?).
test_multi([]).

% Test mixed - pattern match list with tail
split_at_3([1,2,3|Tail], Tail?).
