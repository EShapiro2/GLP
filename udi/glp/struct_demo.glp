% struct_demo.glp - Structure demonstration
%
% Shows how to work with compound terms (structures)

person(alice, age(30), city(seattle)).
person(bob, age(25), city(portland)).

get_age(person(Name?, age(Age?), City), Name, Age) :- true.

get_city(person(Name?, Age, city(City?)), Name, City) :- true.
