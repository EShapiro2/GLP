# Dart Runtime Spec for `@` Operator (Isolate Boot)

**Retired 2026-07-04 (paper-and-code discipline).** The boot realisation (boot/0 with goal@agent, index-0 serializer entry, network-input reader as last argument, Dart routing) is now the IGLP paper appendix `app:in-execution` in `IGLP/sections/appendix-implementation-notes.tex`; the code is `BootLoader`/`IsolateManager` in `glp_runtime`. The UI-agent layer (§4.2: ui_agent_window, ui_agent_actor, ui_relay) is UIVE-owned; route its placement to UIVE. Full text (v0.7) recoverable from git.
