# Agent Execution Spec

**Retired 2026-07-04 (paper-and-code discipline).** The execution model and its invariants (event-driven drain, single re-enqueue path, external termination) are now the IGLP paper appendix `app:in-execution` in `IGLP/sections/appendix-implementation-notes.tex`; the code is `glp_multiagent`/`glp_runtime`. The bug log (§5--6) is history, recoverable from git. Full text recoverable from git.
