# GLP Runtime System Specification

**Retired 2026-07-04 (paper-and-code discipline).** The normative sources are the IGLP paper --- dGLP (Definition dglp-ts, bounded tail), the Implementation Notes appendix `app:in-heap`/`app:in-execution` (heap, clause try with tentative bindings, event-driven execution, tail-budget yield), the code-format appendix `app:code-format` --- and the implemented runtime in `glp_runtime`. Full text (v2.19) recoverable from git.
