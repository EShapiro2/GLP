# GLP Compiler Specification

**Retired 2026-07-04 (stale).** This 2025-11 design draft predates the implemented compiler and contradicts the current code format (it emits assembly labels; the code format has none --- IGLP paper appendix `app:code-format`). Current sources: `parser-spec.md` (front end), `glp-bytecode-v216-complete.md` (instruction semantics), the IGLP paper appendix `app:code-format` (encoding, artefact, loader), and the implemented compiler in `glp_runtime`. Full text recoverable from git.
