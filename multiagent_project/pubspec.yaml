name: multiagent_project
description: GLP Social Graph Play - Multi-Isolate

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0

  # Link to existing GLP runtime
  glp_runtime:
    path: ../glp_runtime

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
