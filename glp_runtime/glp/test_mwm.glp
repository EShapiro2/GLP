% Test - diagnostic for nested structures

% Test with [foo] - atom in list
test1(X?) :-
    bar([foo], X).

% Test with [foo(a)] - structure in list
test2(X?) :-
    bar([foo(a)], X).

bar(L, X?) :-
    X = L?.
