% meta.glp - Meta-interpreter for testing
-module(meta).
exported procedure run(_, _).

run(true, _Module).
run((A, B), Module) :-
    ground(A?), ground(B?), ground(Module?) |
    run(A?, Module?), run(B?, Module?).
run(Goal, Module) :-
    ground(Module?), constant(Module?), otherwise |
    Module? # clause(Goal?, Body),
    run(Body?, Module?).
