%% INVALID: SRSW violation - reader without corresponding writer
%% Mode is CORRECT: Nat? = input (↓), expects reader, Y? is reader ✓
%% But Y? has no corresponding writer Y anywhere in the clause
%% SRSW requires every reader X? to have a corresponding writer X

Nat ::= 0 ; s(Nat).

procedure bar(Nat?).

%% ERROR: Y? is reader but there's no Y writer → SRSW violation
bar(Y?).
